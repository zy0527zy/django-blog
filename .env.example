# DjangoBlog 本地开发环境变量示例
# 使用方法：复制本文件为 .env 并填写 MySQL 密码
# （VSCode 的 Python 插件会自动读取 .env 注入到调试和终端环境）

# MySQL 数据库连接（settings.py 已有默认值，这里仅需覆盖密码）
DJANGO_MYSQL_DATABASE=djangoblog
DJANGO_MYSQL_USER=root
DJANGO_MYSQL_PASSWORD=在此填写你的MySQL密码
DJANGO_MYSQL_HOST=127.0.0.1
DJANGO_MYSQL_PORT=3306
